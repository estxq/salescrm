{
  "name": "schedule-hub",
  "version": "1.0.0",
  "description": "A small in-house CRM for a caller / PA / agent team: contacts, deal pipeline, call logging, email templates, real Zoom scheduling, and a reporting dashboard.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1"
  }
}
